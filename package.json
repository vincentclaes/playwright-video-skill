{
  "name": "playwright-video-skill",
  "version": "1.0.0",
  "private": true,
  "description": "A portable agent skill for clear Playwright recordings",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
